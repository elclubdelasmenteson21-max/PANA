import React from 'react';
import { View } from 'react-native';

const AppNavigator: React.FC = () => {
  return <View style={{ flex: 1, backgroundColor: '#FF6B00' }} />;
};

export default AppNavigator;
